<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <!-- The service holds a partial wake lock: a foreground service keeps the
       process alive, but only a wake lock keeps the CPU running once the screen
       goes off, and without it the polling loop stalls until the next wakeup. -->
  <uses-permission android:name="android.permission.WAKE_LOCK" />

  <application>
    <service
      android:name="expo.modules.foregroundservice.RoutineForegroundService"
      android:exported="false"
      android:foregroundServiceType="dataSync" />
  </application>

</manifest>
